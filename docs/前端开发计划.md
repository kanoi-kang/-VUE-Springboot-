# 文旅电商平台前端开发计划

本文档为新疆文旅产品电子商城平台的前端开发计划，作为后续前端开发的唯一参考文档。

---

## 一、项目架构

### 1.1 项目组成

```
d:\YiJiangNan\
├── backend/                    # 后端服务（已完成）
│   ├── src/main/java/         # Java源码
│   └── sql/                    # 数据库脚本
│
└── frontend/                   # 前端项目
    ├── user-app/               # 用户端（移动端优先，响应式适配PC）
    └── admin-app/              # 管理端（PC端）
```

### 1.2 技术栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Vue 3 + TypeScript | 3.4+ | 组合式API (Composition API) |
| 构建工具 | Vite | 5.0+ | 快速开发体验 |
| UI组件库 | Element Plus | 2.5+ | Vue3主流组件库 |
| 状态管理 | Pinia | 2.1+ | Vue3推荐状态管理 |
| 路由 | Vue Router | 4.2+ | 官方路由 |
| HTTP请求 | Axios | 1.6+ | 请求拦截统一处理 |
| 样式 | SCSS | - | 嵌套样式、变量、混合宏 |
| 图标 | @element-plus/icons-vue | - | Element Plus图标 |
| 工具库 | VueUse | 10.0+ | Vue组合式工具函数 |

### 1.3 后端API服务

- **用户端API基础路径**: `http://localhost:8080/api/user`
- **管理端API基础路径**: `http://localhost:8080/api/admin`
- **Swagger文档**: `http://localhost:8080/swagger-ui.html`

---

## 二、后端接口清单

### 2.1 用户端接口 (`/api/user`)

#### 认证模块 (`/auth`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/auth/captcha` | 获取图形验证码 | 否 |
| POST | `/auth/register` | 用户注册 | 否 |
| POST | `/auth/login` | 用户登录 | 否 |
| GET | `/auth/info` | 获取用户信息 | 是 |

#### 用户模块

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/users/{id}` | 获取用户详情 | 是 |
| PUT | `/users/{id}` | 更新用户信息 | 是 |
| POST | `/users/avatar` | 上传头像 | 是 |

#### 商品模块 (`/products`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/products` | 商品列表（分页、筛选、排序） | 否 |
| GET | `/products/{id}` | 商品详情 | 否 |
| GET | `/products/search` | 搜索商品 | 否 |
| GET | `/products/recommend` | 推荐商品 | 否 |
| GET | `/products/new` | 新品上架 | 否 |
| GET | `/products/hot` | 热门商品 | 否 |

#### 分类模块 (`/categories`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/categories/tree` | 分类树形结构 | 否 |
| GET | `/categories/{id}` | 分类详情 | 否 |
| GET | `/categories/{id}/products` | 分类下的商品 | 否 |

#### 品牌模块 (`/brands`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/brands` | 品牌列表 | 否 |
| GET | `/brands/{id}` | 品牌详情 | 否 |

#### 购物车模块 (`/cart`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/cart` | 获取购物车列表 | 是 |
| POST | `/cart` | 添加商品到购物车 | 是 |
| PUT | `/cart/{id}` | 更新购物车商品数量 | 是 |
| DELETE | `/cart/{id}` | 删除购物车商品 | 是 |
| DELETE | `/cart/clear` | 清空购物车 | 是 |

#### 订单模块 (`/orders`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/orders` | 订单列表 | 是 |
| GET | `/orders/{id}` | 订单详情 | 是 |
| POST | `/orders` | 创建订单 | 是 |
| PUT | `/orders/{id}/cancel` | 取消订单 | 是 |
| PUT | `/orders/{id}/confirm` | 确认收货 | 是 |
| PUT | `/orders/{id}/pay` | 支付订单 | 是 |

#### 收藏模块 (`/collections`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/collections` | 我的收藏列表 | 是 |
| POST | `/collections/{productId}` | 添加收藏 | 是 |
| DELETE | `/collections/{productId}` | 取消收藏 | 是 |
| GET | `/collections/check/{productId}` | 检查是否收藏 | 是 |

#### 评价模块 (`/reviews`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/reviews/product/{productId}` | 商品评价列表 | 否 |
| GET | `/reviews/product/{productId}/stats` | 商品评价统计 | 否 |
| GET | `/reviews/user` | 我的评价列表 | 是 |
| POST | `/reviews` | 提交评价 | 是 |

#### 优惠券模块 (`/coupons`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/coupons/available` | 可领取优惠券 | 否 |
| GET | `/coupons/my` | 我的优惠券 | 是 |
| POST | `/coupons/receive/{couponId}` | 领取优惠券 | 是 |
| GET | `/coupons/calculate` | 计算优惠金额 | 是 |

#### 收货地址模块 (`/addresses`)

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/addresses` | 地址列表 | 是 |
| GET | `/addresses/{id}` | 地址详情 | 是 |
| POST | `/addresses` | 新增地址 | 是 |
| PUT | `/addresses/{id}` | 更新地址 | 是 |
| DELETE | `/addresses/{id}` | 删除地址 | 是 |
| PUT | `/addresses/{id}/default` | 设置默认地址 | 是 |

---

### 2.2 管理端接口 (`/api/admin`)

#### 管理员认证

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/captcha` | 获取图形验证码 | 否 |
| POST | `/login` | 管理员登录 | 否 |

#### 商品管理

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/products` | 商品列表 | 是 |
| GET | `/products/{id}` | 商品详情 | 是 |
| POST | `/products` | 新增商品 | 是 |
| PUT | `/products/{id}` | 更新商品 | 是 |
| DELETE | `/products/{id}` | 删除商品 | 是 |
| PUT | `/products/{id}/publish` | 发布/下架 | 是 |

#### 分类管理

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/categories` | 分类列表 | 是 |
| GET | `/categories/tree` | 分类树 | 是 |
| POST | `/categories` | 新增分类 | 是 |
| PUT | `/categories/{id}` | 更新分类 | 是 |
| DELETE | `/categories/{id}` | 删除分类 | 是 |

#### 品牌管理

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/brands` | 品牌列表 | 是 |
| POST | `/brands` | 新增品牌 | 是 |
| PUT | `/brands/{id}` | 更新品牌 | 是 |
| DELETE | `/brands/{id}` | 删除品牌 | 是 |

#### 订单管理

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/orders` | 订单列表 | 是 |
| GET | `/orders/{id}` | 订单详情 | 是 |
| PUT | `/orders/{id}/ship` | 发货 | 是 |
| PUT | `/orders/{id}/refund` | 退款 | 是 |

#### 优惠券管理

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/coupons` | 优惠券列表 | 是 |
| POST | `/coupons` | 新增优惠券 | 是 |
| PUT | `/coupons/{id}` | 更新优惠券 | 是 |
| DELETE | `/coupons/{id}` | 删除优惠券 | 是 |

#### 用户管理

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| GET | `/users` | 用户列表 | 是 |
| GET | `/users/{id}` | 用户详情 | 是 |
| PUT | `/users/{id}/status` | 禁用/启用用户 | 是 |

---

## 三、数据库设计（参考）

### 3.1 核心表结构

#### 用户相关
- `users` - 用户表
- `admins` - 管理员表
- `user_addresses` - 收货地址表

#### 商品相关
- `categories` - 商品分类表（树形结构）
- `brands` - 品牌表
- `products` - 商品表
- `product_pics` - 商品图片表

#### 交易相关
- `cart_items` - 购物车表
- `orders` - 订单表
- `order_items` - 订单商品表

#### 营销相关
- `collections` - 用户收藏表
- `reviews` - 商品评价表
- `coupons` - 优惠券表
- `coupon_users` - 用户优惠券表

### 3.2 分类体系（文旅特色）

```
新疆美食
├── 干果坚果（葡萄干、巴旦木、核桃）
├── 特色零食（烤包子、馕、手抓饭）
└── 粮油调味（胡麻油、番茄酱）

节日礼品
├── 春节礼盒
└── 中秋礼盒

珠宝玉器
├── 和田玉
└── 金银首饰

时尚配饰
├── 民族饰品
└── 手工挂件

雕刻塑造
├── 木雕工艺
└── 泥塑艺术
```

---

## 四、用户端页面清单

### 4.1 页面列表

| 页面路径 | 页面名称 | 功能描述 | 优先级 |
|---------|---------|---------|--------|
| `/` | 首页 | 轮播图、分类入口、推荐商品、新品 | 高 |
| `/category` | 分类页 | 左侧分类列表、右侧商品 | 高 |
| `/products` | 商品列表 | 搜索、筛选、排序、分页 | 高 |
| `/product/:id` | 商品详情 | 轮播图、规格、评价、收藏、购物车 | 高 |
| `/cart` | 购物车 | 商品列表、数量、删除、结算 | 高 |
| `/checkout` | 确认订单 | 地址、优惠券、提交订单 | 高 |
| `/pay/:orderId` | 支付页 | 支付方式、支付结果 | 中 |
| `/user` | 个人中心 | 用户信息、订单入口、功能菜单 | 中 |
| `/orders` | 订单列表 | 全部/待付款/待发货/待收货/已完成 | 中 |
| `/order/:id` | 订单详情 | 订单信息、物流、操作按钮 | 中 |
| `/collections` | 我的收藏 | 收藏商品列表 | 低 |
| `/coupons` | 我的优惠券 | 可用/已用/已过期 | 低 |
| `/addresses` | 地址管理 | 地址列表、增删改查 | 中 |
| `/address/edit/:id` | 地址编辑 | 新增/编辑地址 | 中 |
| `/review/:orderId` | 评价页 | 提交评价、上传图片 | 低 |
| `/login` | 登录页 | 账号登录、图形验证码 | 高 |
| `/register` | 注册页 | 账号注册、图形验证码 | 高 |

### 4.2 用户端路由结构

```typescript
// /user-app/src/router/index.ts
routes: [
  // 公共页面
  { path: '/', name: 'Home', component: () => import('@/views/home/index.vue') },
  { path: '/category', name: 'Category', component: () => import('@/views/category/index.vue') },
  { path: '/products', name: 'ProductList', component: () => import('@/views/product/list.vue') },
  { path: '/product/:id', name: 'ProductDetail', component: () => import('@/views/product/detail.vue') },

  // 需要登录的页面
  { path: '/cart', name: 'Cart', component: () => import('@/views/cart/index.vue'), meta: { requiresAuth: true } },
  { path: '/checkout', name: 'Checkout', component: () => import('@/views/order/checkout.vue'), meta: { requiresAuth: true } },
  { path: '/pay/:orderId', name: 'Pay', component: () => import('@/views/order/pay.vue'), meta: { requiresAuth: true } },
  { path: '/user', name: 'User', component: () => import('@/views/user/index.vue'), meta: { requiresAuth: true } },
  { path: '/orders', name: 'OrderList', component: () => import('@/views/order/list.vue'), meta: { requiresAuth: true } },
  { path: '/order/:id', name: 'OrderDetail', component: () => import('@/views/order/detail.vue'), meta: { requiresAuth: true } },
  { path: '/collections', name: 'Collections', component: () => import('@/views/user/collections.vue'), meta: { requiresAuth: true } },
  { path: '/coupons', name: 'Coupons', component: () => import('@/views/user/coupons.vue'), meta: { requiresAuth: true } },
  { path: '/addresses', name: 'Addresses', component: () => import('@/views/user/addresses.vue'), meta: { requiresAuth: true } },
  { path: '/address/edit/:id?', name: 'AddressEdit', component: () => import('@/views/user/address-edit.vue'), meta: { requiresAuth: true } },
  { path: '/review/:orderId', name: 'Review', component: () => import('@/views/user/review.vue'), meta: { requiresAuth: true } },

  // 认证页面
  { path: '/login', name: 'Login', component: () => import('@/views/auth/login.vue') },
  { path: '/register', name: 'Register', component: () => import('@/views/auth/register.vue') },
]
```

---

## 五、管理端页面清单

### 5.1 页面列表

| 页面路径 | 页面名称 | 功能描述 | 优先级 |
|---------|---------|---------|--------|
| `/login` | 登录页 | 管理员登录、图形验证码 | 高 |
| `/` | 仪表盘 | 数据统计、图表展示 | 中 |
| `/products` | 商品管理 | 商品列表、筛选、新增、编辑 | 高 |
| `/products/create` | 新增商品 | 商品表单 | 高 |
| `/products/edit/:id` | 编辑商品 | 商品表单 | 高 |
| `/categories` | 分类管理 | 树形列表、新增、编辑、删除 | 高 |
| `/brands` | 品牌管理 | 品牌列表、新增、编辑、删除 | 中 |
| `/orders` | 订单管理 | 订单列表、筛选、详情 | 高 |
| `/orders/:id` | 订单详情 | 订单信息、发货、退款 | 高 |
| `/coupons` | 优惠券管理 | 优惠券列表、新增、编辑、删除 | 中 |
| `/coupons/create` | 新增优惠券 | 优惠券表单 | 中 |
| `/coupons/edit/:id` | 编辑优惠券 | 优惠券表单 | 中 |
| `/users` | 用户管理 | 用户列表、查看、禁用 | 低 |
| `/users/:id` | 用户详情 | 用户信息、订单记录 | 低 |

### 5.2 管理端路由结构

```typescript
// /admin-app/src/router/index.ts
routes: [
  { path: '/login', name: 'Login', component: () => import('@/views/login/index.vue') },

  {
    path: '/',
    component: () => import('@/layouts/index.vue'),
    meta: { requiresAuth: true },
    children: [
      { path: '', name: 'Dashboard', component: () => import('@/views/dashboard/index.vue') },
      { path: 'products', name: 'ProductList', component: () => import('@/views/product/list.vue') },
      { path: 'products/create', name: 'ProductCreate', component: () => import('@/views/product/form.vue') },
      { path: 'products/edit/:id', name: 'ProductEdit', component: () => import('@/views/product/form.vue') },
      { path: 'categories', name: 'CategoryList', component: () => import('@/views/category/list.vue') },
      { path: 'brands', name: 'BrandList', component: () => import('@/views/brand/list.vue') },
      { path: 'orders', name: 'OrderList', component: () => import('@/views/order/list.vue') },
      { path: 'orders/:id', name: 'OrderDetail', component: () => import('@/views/order/detail.vue') },
      { path: 'coupons', name: 'CouponList', component: () => import('@/views/coupon/list.vue') },
      { path: 'coupons/create', name: 'CouponCreate', component: () => import('@/views/coupon/form.vue') },
      { path: 'coupons/edit/:id', name: 'CouponEdit', component: () => import('@/views/coupon/form.vue') },
      { path: 'users', name: 'UserList', component: () => import('@/views/user/list.vue') },
      { path: 'users/:id', name: 'UserDetail', component: () => import('@/views/user/detail.vue') },
    ]
  }
]
```

---

## 六、项目结构

### 6.1 用户端结构

```
/frontend/user-app/
├── public/
│   └── favicon.ico
├── src/
│   ├── api/                    # API接口
│   │   ├── index.ts            # API统一导出
│   │   ├── auth.ts            # 认证接口
│   │   ├── user.ts            # 用户接口
│   │   ├── product.ts         # 商品接口
│   │   ├── cart.ts           # 购物车接口
│   │   ├── order.ts          # 订单接口
│   │   ├── collection.ts     # 收藏接口
│   │   ├── review.ts         # 评价接口
│   │   ├── coupon.ts         # 优惠券接口
│   │   └── address.ts        # 地址接口
│   │
│   ├── assets/                 # 静态资源
│   │   ├── images/           # 图片
│   │   └── styles/          # 样式
│   │       └── variables.scss
│   │
│   ├── components/             # 公共组件
│   │   ├── common/           # 通用组件
│   │   ├── product/          # 商品组件
│   │   ├── order/            # 订单组件
│   │   └── layout/           # 布局组件
│   │
│   ├── composables/            # 组合式函数
│   │
│   ├── layouts/                # 页面布局
│   │   ├── DefaultLayout.vue
│   │   └── UserLayout.vue
│   │
│   ├── router/                 # 路由配置
│   │
│   ├── stores/                 # 状态管理
│   │   ├── user.ts           # 用户状态
│   │   ├── cart.ts           # 购物车状态
│   │   └── app.ts            # 应用状态
│   │
│   ├── types/                  # TypeScript类型
│   │
│   ├── utils/                  # 工具函数
│   │   ├── request.ts        # Axios封装
│   │   ├── storage.ts        # 本地存储
│   │   └── format.ts         # 格式化函数
│   │
│   ├── views/                  # 页面组件
│   │   ├── home/
│   │   ├── category/
│   │   ├── product/
│   │   ├── cart/
│   │   ├── order/
│   │   ├── user/
│   │   └── auth/
│   │
│   ├── App.vue
│   └── main.ts
│
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
└── .env
```

### 6.2 管理端结构

```
/frontend/admin-app/
├── public/
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   │   ├── common/
│   │   └── form/
│   ├── composables/
│   ├── layouts/                # 管理端布局
│   │   ├── index.vue
│   │   ├── Sidebar.vue
│   │   └── Header.vue
│   ├── router/
│   ├── stores/
│   ├── types/
│   ├── utils/
│   ├── views/
│   │   ├── login/
│   │   ├── dashboard/
│   │   ├── product/
│   │   ├── category/
│   │   ├── brand/
│   │   ├── order/
│   │   ├── coupon/
│   │   └── user/
│   ├── App.vue
│   └── main.ts
│
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
└── .env
```

---

## 七、API接口封装

### 7.1 Axios请求封装

```typescript
// utils/request.ts
import axios from 'axios'
import type { AxiosInstance, AxiosResponse } from 'axios'
import { ElMessage } from 'element-plus'

const request: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
})

request.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => Promise.reject(error)
)

request.interceptors.response.use(
  (response: AxiosResponse) => {
    const { code, message, data } = response.data
    if (code === 200 || code === 0) {
      return data
    }
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    ElMessage.error(error.response?.data?.message || '网络错误')
    return Promise.reject(error)
  }
)

export default request
```

### 7.2 API接口示例

```typescript
// api/auth.ts
import request from '@/utils/request'

export const getCaptcha = () => {
  return request.get('/user/auth/captcha', { responseType: 'blob' })
}

export const login = (data: { username: string; password: string; captcha: string }) => {
  return request.post('/user/auth/login', data)
}

export const register = (data: { username: string; password: string; captcha: string }) => {
  return request.post('/user/auth/register', data)
}

export const getUserInfo = () => {
  return request.get('/user/auth/info')
}
```

---

## 八、状态管理

```typescript
// stores/user.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { login as apiLogin, getUserInfo } from '@/api/auth'

export const useUserStore = defineStore('user', () => {
  const token = ref(localStorage.getItem('token') || '')
  const userInfo = ref(null)

  const login = async (username: string, password: string, captcha: string) => {
    const data = await apiLogin({ username, password, captcha })
    token.value = data.token
    localStorage.setItem('token', data.token)
    return data
  }

  const fetchUserInfo = async () => {
    if (!token.value) return null
    const data = await getUserInfo()
    userInfo.value = data
    return data
  }

  const logout = () => {
    token.value = ''
    userInfo.value = null
    localStorage.removeItem('token')
  }

  return { token, userInfo, login, fetchUserInfo, logout }
})
```

---

## 九、路由守卫

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router'
import { useUserStore } from '@/stores/user'

const router = createRouter({
  history: createWebHistory(),
  routes: []
})

router.beforeEach((to, from, next) => {
  const userStore = useUserStore()

  if (to.meta.requiresAuth && !userStore.token) {
    next({ name: 'Login', query: { redirect: to.fullPath } })
  } else {
    next()
  }
})

export default router
```

---

## 十、开发阶段

### 第一阶段：项目初始化（1-2天）

**用户端**
- [ ] 创建Vue 3 + Vite + TypeScript项目
- [ ] 配置Vite、Element Plus、Axios、路由、Pinia
- [ ] 创建基础布局组件
- [ ] 创建API接口模块
- [ ] 创建图形验证码组件

**管理端**
- [ ] 创建Vue 3 + Vite + TypeScript项目
- [ ] 配置Vite、Element Plus、Axios、路由、Pinia
- [ ] 创建后台管理布局
- [ ] 创建API接口模块

---

### 第二阶段：用户端核心页面（3-5天）

| 页面 | 优先级 |
|------|--------|
| 登录/注册（含图形验证码） | 高 |
| 首页 | 高 |
| 分类页 | 高 |
| 商品列表页 | 高 |
| 商品详情页 | 高 |
| 购物车页 | 高 |
| 确认订单页 | 高 |
| 支付页 | 中 |
| 个人中心 | 中 |
| 订单列表页 | 中 |
| 订单详情页 | 中 |
| 收藏列表 | 低 |
| 优惠券页 | 低 |
| 地址管理 | 中 |
| 评价页 | 低 |

---

### 第三阶段：管理端核心页面（3-5天）

| 页面 | 优先级 |
|------|--------|
| 登录页（含图形验证码） | 高 |
| 仪表盘 | 中 |
| 商品管理 | 高 |
| 分类管理 | 高 |
| 品牌管理 | 中 |
| 订单管理 | 高 |
| 优惠券管理 | 中 |
| 用户管理 | 低 |

---

### 第四阶段：功能完善与优化（2-3天）

- [ ] 用户端响应式适配
- [ ] 图片懒加载
- [ ] 页面加载骨架屏
- [ ] 错误页面（404、500）
- [ ] 权限控制完善
- [ ] 性能优化
- [ ] 代码优化与重构

---

## 十一、备注

1. **图形验证码**：后端生成方案，由后端提供 `/captcha` 接口返回验证码图片
2. **支付功能**：微信/支付宝支付集成，后续扩展
3. **图片上传**：使用七牛云/阿里云OSS，后续扩展
4. **实时通讯**：如需消息通知，使用WebSocket，后续扩展

---

*文档版本：v1.0*
*最后更新：2026-05-03*
